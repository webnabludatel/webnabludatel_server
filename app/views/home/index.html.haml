%h1.b-mainhead
  Мобильное приложение
  %br/
  %strong.b-mainhead__name Веб-наблюдатель
  = image_tag("logo.png", :class => "b-mainhead__logo")

  %p
    = render :partial => "partials/share"
%ul.b-marklist
  %li.b-marklist__item Содержит список всех возможных нарушений для проверки участка.
  %li.b-marklist__item Поможет составить и подать жалобу по замеченным нарушениям.
  %li.b-marklist__item.b-marklist__item_double Поддерживает загрузку фото и видео доказательств в любой удобный момент.
  %li.b-marklist__item Со встроенным справочниками наблюдателя ЦИК и организации «Голос».
.b-know
  - if defined?(@subscriber) && @subscriber.persisted?
    %b Спасибо! Уже скоро мы сообщим вам о готовности нашего приложения.
  - else
    %form.b-know__form{:action => subscribe_path, :method => "POST"}
      %fieldset.b-know__set
        %legend.b-know__name Узнайте о выходе приложения для Android первыми
        - if defined?(@subscriber) && !@subscriber.valid?
          %ul
            - @subscriber.errors.full_messages.each do |message|
              %li= message

        %input.b-know__input{:placeholder => "@адрес", :type => "text", :name => "email", :value => (defined?(@subscriber) ? @subscriber.email : "")}/
        %button.b-know__button Подписаться
.b-download
  Скачайте
  %a.b-download__apple{:href => "http://itunes.apple.com/ru/app/nabludatel/id505350851?ls=1&mt=8"} в Аппсторе
-#  или на
  %a.b-download__android{:href => "#"}
    %span.b-download__android-name> Android
    \-маркете

- content_for :sidebar do
  .b-gallery
    %ul#slidingFeatures.b-gallery__list
      / Все картинки должны быть размером 209*311px, PNG24
      %li.b-gallery__item
        %a.b-gallery__link{:href => "#"}
          %img{:alt => "", :src => image_path("slides/slide-1.jpg") }/
      %li.b-gallery__item
        %a.b-gallery__link{:href => "#"}
          %img{:alt => "", :src => image_path("slides/slide-2.jpg")}/
      %li.b-gallery__item
        %a.b-gallery__link{:href => "#"}
          %img{:alt => "", :src => image_path("slides/slide-3.jpg")}/
    .b-gallery__phone
    /
      <div class="b-gallery__control b-gallery__control_left b-gallery__control_left-passive"></div>
      <div class="b-gallery__control b-gallery__control_right"></div>

:javascript
  $(document).ready(function(){ $('#slidingFeatures').jshowoff({
    effect: 'slideLeft',
    hoverPause: true,
    links: false,
    controlText:{play:'',pause:'',previous:'',next:''},
    controls: true
  }); });